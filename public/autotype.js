$(document).ready(function(){
   
  var typed = new Typed('#typed',{
    strings:["Disaster Management"],
    backSpeed: 100,
    typeSpeed: 150,
    smartbackspace:true,
    cursorChar: ' ',
    loop:true
  });
 
  var type = new Typed('#footer',{
    strings:["by Quad X"],
    backSpeed: 100,
    typeSpeed: 100,
    smartbackspace:true,
    cursorChar: ' ',
    loop:true
  });


});